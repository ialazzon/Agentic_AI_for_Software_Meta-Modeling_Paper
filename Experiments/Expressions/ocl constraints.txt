self.functions->size() = 3
self.functions->exists(f | f.name = 'AdditionFunction' and f.parameters->size() = 2 and f.parameters->exists(p | p.name = 'x') and f.parameters->exists(p | p.name = 'y') and f.body.oclIsKindOf(Plus) and f.body.oclAsType(Plus).op1.oclIsKindOf(ParameterAccess) and f.body.oclAsType(Plus).op1.oclAsType(ParameterAccess).parameter.name = 'x' and f.body.oclAsType(Plus).op2.oclIsKindOf(ParameterAccess) and f.body.oclAsType(Plus).op2.oclAsType(ParameterAccess).parameter.name = 'y')
self.functions->exists(f | f.name = 'MultiplicationFunction' and f.parameters->size() = 2 and f.parameters->exists(p | p.name = 'a') and f.parameters->exists(p | p.name = 'b') and f.body.oclIsKindOf(Mul) and f.body.oclAsType(Mul).op1.oclIsKindOf(ParameterAccess) and f.body.oclAsType(Mul).op1.oclAsType(ParameterAccess).parameter.name = 'a' and f.body.oclAsType(Mul).op2.oclIsKindOf(ParameterAccess) and f.body.oclAsType(Mul).op2.oclAsType(ParameterAccess).parameter.name = 'b')
self.functions->exists(f | f.name = 'NegationFunction' and f.parameters->size() = 1 and f.parameters->exists(p | p.name = 'value') and f.body.oclIsKindOf(Neg) and f.body.oclAsType(Neg).op.oclIsKindOf(ParameterAccess) and f.body.oclAsType(Neg).op.oclAsType(ParameterAccess).parameter.name = 'value')